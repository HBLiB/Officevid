# Round 2 — Frontend

## Task

Add the H-Network logo to the login page.

## Requirements

1. Add an H-Network logo above the login form title
2. Use an SVG or CSS-based logo (no external image files needed) — or create a small image asset in `frontend/` if preferred
3. Keep it visually balanced with the existing centered card layout

## Scope

Only touch files inside `frontend/`.
